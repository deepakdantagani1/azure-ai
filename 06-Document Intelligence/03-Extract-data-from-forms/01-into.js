/**
 * Azure Document Intelligence uses Optical Character Recognition (OCR) capabilities and deep learning models to extract text, 
 * key-value pairs, selection marks, and tables from documents.
 * 
 * ==> OCR captures document structure by creating bounding boxes around detected objects in an image
 */

/**
 * Azure Document Intelligence is composed of the following services:
 * ==> Document analysis models
 * ==> Prebuilt models:
 * ==> Custom models
 */