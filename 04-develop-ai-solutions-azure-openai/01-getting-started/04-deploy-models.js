/**
 * deploy any number of deployments in one or multiple Azure OpenAI resources as long as their 
 * Tokens Per Minute (TPM) stays within the deployment quota.
 * 
 * deployment using, azure openai studio, api or cli
 */